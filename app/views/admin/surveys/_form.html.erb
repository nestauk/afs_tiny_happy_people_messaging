<%= form_with(model: @survey, local: true, url: admin_surveys_path) do |form| %>
  <div class="mb-4">
    <%= form.label :title, class: "block text-sm font-medium text-gray-700" %>
    <%= form.text_field :title, class: "mt-1 block w-full border border-gray-300 rounded-md shadow-sm focus:ring focus:ring-blue-500" %>
  </div>

  <div class="mb-4">
    <%= form.submit "Create survey", class: "bg-blue-500 text-white rounded p-2" %>
  </div>
<% end %>
