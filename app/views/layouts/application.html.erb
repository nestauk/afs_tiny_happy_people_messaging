<!DOCTYPE html>
<html lang="en">
  <head>
    <title><%= content_for?(:page_title) ? yield(:page_title) + " | #{I18n.t('titles.application')}" : I18n.t('titles.application') %></title>
    <meta name="viewport" content="width=device-width,initial-scale=1">
    <meta name="description" content="Free texts sent weekly to your phone for fun activities to try out with your child from BBC's Tiny Happy People">
    <%= csrf_meta_tags %>
    <%= csp_meta_tag %>
    <meta name="turbo-prefetch" content="false">
    <meta name="turbo-cache-control" content="no-preview">

    <%= vite_client_tag %>
    <%= vite_stylesheet_tag 'application', media: 'all' %>
    <%= vite_typescript_tag 'application' %>
    <%= content_for :scripts %>
  </head>

  <body>
    <%= render "layouts/navbar" %>
    <main class="w-full" role="main" id="main">
      <% if current_admin && @hide_sidebar.blank? %>
        <div class="grid grid-cols-4 h-svh">
          <%= render "layouts/sidebar" %>
          <div class="mt-10 col-span-3 px-20">
            <% if flash[:notice] %>
              <div class="px-3 py-5 bg-green-300 mb-10">
                <p class="">
                  <%= flash[:notice] %>
                </p>
              </div>
            <% end %>
            <%= yield %>
          </div>
        </div>
      <% else %>
        <% if flash[:notice] %>
          <div class="md:px-28 px-3 py-5">
            <p class="">
              <%= flash[:notice] %>
            </p>
          </div>
        <% end %>
        <%= yield %>
      <% end %>
    </main>
    <% if @show_footer %>
      <%= render "layouts/footer" %>
    <% end %>
  </body>
</html>
