<%= simple_form_for @group do |form| %>
  <div class="mt-3">
    <%= form.input :name %>
  </div>

  <%= form.submit (action_name == "edit" ? "Update" : "Create"), class: "btn btn-primary mt-5 bg-bbc-blue text-white w-full" %>
<% end %>
